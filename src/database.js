const firebaseConfig={},firebase=require("firebase"),bancodedados=require("shorten-firebase.realtime-database");try{firebase.initializeApp(firebaseConfig),console.log("Banco de Dados conectado, com index em [googleId]!")}catch(e){console.log(e)}const database=new bancodedados(firebase,!1);module.exports={database,firebase};
